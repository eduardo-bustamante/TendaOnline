@model TendaOnline.Models.ViewModels.RegistrarUsuarioViewModel

@{
    ViewData["Title"] = "Criar Novo Usuário";
}

<div class="row justify-content-center">
    <div class="col-md-6 col-lg-5">
        <div class="card border-0 shadow-sm">
            <div class="card-header bg-primary text-white py-3">
                <h5 class="card-title mb-0 fw-bold">
                    <i class="bi bi-person-plus-fill me-2"></i> Novo Usuário de Acesso
                </h5>
            </div>
            <div class="card-body p-4">
                <form asp-action="Registrar" method="post">
                    @Html.AntiForgeryToken()
                    <div asp-validation-summary="ModelOnly" class="alert alert-danger small py-2 mb-3"></div>

                    <div class="mb-3">
                        <label asp-for="Email" class="form-label fw-bold">E-mail de Login</label>
                        <input asp-for="Email" class="form-control" placeholder="ex: funcionario@tendaonline.com" required />
                        <span asp-validation-for="Email" class="text-danger small"></span>
                    </div>

                    <div class="mb-3">
                        <label asp-for="Perfil" class="form-label fw-bold">Nível de Permissão</label>
                        <select asp-for="Perfil" class="form-select" required>
                            <option value="Operador">Operador (Apenas PDV e Vendas)</option>
                            <option value="Admin">Administrador (Acesso Irrestrito)</option>
                        </select>
                        <span asp-validation-for="Perfil" class="text-danger small"></span>
                    </div>

                    <div class="mb-3">
                        <label asp-for="Senha" class="form-label fw-bold">Senha Provisória</label>
                        <input asp-for="Senha" class="form-control" placeholder="Mínimo de 6 dígitos" required />
                        <span asp-validation-for="Senha" class="text-danger small"></span>
                    </div>

                    <div class="mb-4">
                        <label asp-for="ConfirmarSenha" class="form-label fw-bold">Confirmar Senha</label>
                        <input asp-for="ConfirmarSenha" class="form-control" placeholder="Repita a senha" required />
                        <span asp-validation-for="ConfirmarSenha" class="text-danger small"></span>
                    </div>

                    <div class="d-flex justify-content-end gap-2 pt-3 border-top">
                        <a asp-action="Usuarios" class="btn btn-outline-secondary">Cancelar</a>
                        <button type="submit" class="btn btn-primary px-4 fw-semibold">Criar Usuário</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{ await Html.RenderPartialAsync("_ValidationScriptsPartial"); }
}